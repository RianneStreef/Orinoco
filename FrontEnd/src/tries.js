let id = 10;
let itemsArr = [1, 3, 5, 10];

// export function removeItem(id, itemsArr) {
//   const newCollection = itemsArr.filter((item) => item !== id);
//   return newCollection;
// }

export function removeItem() {
  let item = 10;
  return item;
}

removeItem();
